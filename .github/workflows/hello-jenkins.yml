name: Hello-Jenkins

on: 
  workflow_dispatch: # This enables the "Run workflow" button

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello Sivanesh!"
        
      - name: Create Lab File
        run: echo "This is my Task 3 lab report" > lab_report.txt
        
      - name: List Workspace Files
        run: ls -R
